import { BasicIconProps } from '../../types';
import { renderBasicIcon } from '../../utils';
import Play from './Play';

export const PlayIcon = (props: BasicIconProps) => {
  return renderBasicIcon(props, Play);
};
