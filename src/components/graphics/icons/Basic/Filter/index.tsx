import { BasicIconProps } from '../../types';
import { renderBasicIcon } from '../../utils';
import Filter from './Filter';

export const FilterIcon = (props: BasicIconProps) => {
  return renderBasicIcon(props, Filter);
};
