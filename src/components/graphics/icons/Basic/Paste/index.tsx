import { BasicIconProps } from '../../types';
import { renderBasicIcon } from '../../utils';
import Paste from './Paste';

export const PasteIcon = (props: BasicIconProps) => renderBasicIcon(props, Paste);
