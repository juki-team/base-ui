import { BasicIconProps } from '../../types';
import { renderBasicIcon } from '../../utils';
import Share from './Share';

export const ShareIcon = (props: BasicIconProps) => {
  return renderBasicIcon(props, Share);
};
