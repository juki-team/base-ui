import { BasicIconProps } from '../../types';
import { renderBasicIcon } from '../../utils';
import Extension from './Extension';

export const ExtensionIcon = (props: BasicIconProps) => {
  return renderBasicIcon(props, Extension);
};
