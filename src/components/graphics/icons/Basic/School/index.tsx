import { BasicIconProps } from '../../types';
import { renderBasicIcon } from '../../utils';
import School from './School';

export const SchoolIcon = (props: BasicIconProps) => {
  return renderBasicIcon(props, School);
};
