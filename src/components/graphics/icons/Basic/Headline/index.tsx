import { BasicIconProps } from '../../types';
import { renderBasicIcon } from '../../utils';
import Headline from './Headline';

export const HeadlineIcon = (props: BasicIconProps) => renderBasicIcon(props, Headline);
