import { BasicIconProps } from '../../types';
import { renderBasicIcon } from '../../utils';
import CloudUpload from './CloudUpload';

export const CloudUploadIcon_ = (props: BasicIconProps) => {
  return renderBasicIcon(props, CloudUpload);
};
