import { BasicIconProps } from '../../types';
import { renderBasicIcon } from '../../utils';
import AtSign from './AtSign';

export const AtSignIcon = (props: BasicIconProps) => renderBasicIcon(props, AtSign);
