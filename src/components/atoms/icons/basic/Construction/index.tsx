import { BasicIconProps } from '../../types';
import { renderBasicIcon } from '../../utils';
import Construction from './Construction';

export const ConstructionIcon = (props: BasicIconProps) => renderBasicIcon(props, Construction);
