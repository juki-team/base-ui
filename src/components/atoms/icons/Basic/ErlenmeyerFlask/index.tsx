import { BasicIconProps } from '../../types';
import { renderBasicIcon } from '../../utils';
import ErlenmeyerFlask from './ErlenmeyerFlask';

export const ErlenmeyerFlaskIcon = (props: BasicIconProps) => renderBasicIcon(props, ErlenmeyerFlask);
